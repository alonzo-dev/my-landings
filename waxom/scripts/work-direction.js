'use strict';

const workDirectionItems = document.querySelectorAll('.work_direction li');
const workDirectionIcons = document.querySelectorAll('.work_direction img');



for (let i = 0; i < workDirectionItems.length; i++) {
	workDirectionItems[i].addEventListener('mouseover', function () {

	});
}